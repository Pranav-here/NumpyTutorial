# We use the sort() function, numbers, aplhabets

import numpy as np

arr = np.array([2,3,3,1,0])
newarr = np.sort(arr)
print(newarr)

arr = np.array([True, False, True])
print(np.sort(arr))


arr = np.array([[3, 2, 4], [5, 0, 1]])
print(np.sort(arr))
